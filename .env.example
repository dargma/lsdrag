# .env.example — 실제 .env는 반드시 .gitignore. 키 평문 커밋 금지.
# 복사: cp .env.example .env   그 후 값 채우기

# Upstage Document Parse (파서) — 사용자 환경에서 이미 쓰는 이름
UP_TOKEN=

# OpenAI (Reader = GPT-4.1 mini, 멀티모달/VLM 겸용)
OPENAI_API_KEY=
